Optional managed-write approval: request_mutation_approval(skill_name, params, connection_id) prepares one managed single-statement proposal and asks the Host to collect human approval using MCP 2026-07-28 MRTR. It never authorizes imperative Skills. Preserve the exact target and parameters across continuations. An unsupported client must use the existing preview/execute workflow with trusted Host approval. Do not treat an approval request as a completed write. Never automatically retry a write after a timeout, lost response, consumed token, or unknown outcome; first reconcile the database state with the operator.
